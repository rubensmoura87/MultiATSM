# Prevents algorithm to end up in ill-defined likelihood

Prevents algorithm to end up in ill-defined likelihood

## Usage

``` r
ML_stable(par, MLvec)
```

## Arguments

- par:

  vector of parameters

- MLvec:

  objective function
